# java 데이터 컬렉션

# Java 데이터 컬렉션 (Collection Framework)

데이터를 여러 개 한 번에 다루는 도구

(Collection = 수집, 모음)

---

## 왜 배열 대신 컬렉션을 쓸까?

| 구분 | 배열(Array) | 컬렉션(Collection) |
| --- | --- | --- |
| 크기 | 고정됨 | 자동으로 늘어남 |
| 데이터 삭제 | 직접 인덱스 조정해야 함 | `remove()`로 간단히 가능 |
| 정렬·탐색 | 직접 구현 | 유틸리티 메서드 제공 |
| 자료형 제한 | 한 타입만 가능 | **제네릭(Generic)** 으로 유연하게 가능 |

---

## 전체 구조

```
Collection
├── List (순서 O, 중복 O)
│   ├── ArrayList
│   ├── LinkedList
│   └── Vector / Stack
│
├── Set (순서 X, 중복 X)
│   ├── HashSet
│   └── TreeSet
│
└── Map (Key-Value 구조)
    ├── HashMap
    └── TreeMap

```

---

## 핵심 요약표

| 인터페이스 | 순서 | 중복 | 형태 | 대표 클래스 |
| --- | --- | --- | --- | --- |
| **List** | O | O | `[값, 값, 값]` | `ArrayList` |
| **Set** | X | X | `{값, 값}` | `HashSet` |
| **Map** | Key 기준 | Key ❌ / Value ✅ | `{key=value}` | `HashMap` |

---

## 코드 예시

```java
// List 예시 (쇼핑카트)
List<String> cart = new ArrayList<>();
cart.add("사과");
cart.add("샤인머스켓");
cart.add("사과");

System.out.println(cart); // [사과, 샤인머스켓, 사과]

// Set 예시 (1인 1개 한정상품 / 중복 불가)
Set<String> limited = new HashSet<>();
limited.add("이벤트 한정 상품");
limited.add("이벤트 한정 상품"); // 중복 무시

System.out.println(limited); // [이벤트 한정 상품]

// Map 예시 (매장 안내도)
Map<String, String> store = new HashMap<>();
store.put("과일코너", "사과");
store.put("냉동코너", "하겐다즈");

System.out.println(store.get("냉동코너")); // 하겐다즈

```

---

## 제네릭(Generic)

컬렉션에 어떤 타입의 데이터를 담을지 미리 지정하는 문법

→ 컬렉션의 **자료형 안전벨트**

---

### 제네릭을 쓰지 않으면 (옛날 방식)

```java
List list = new ArrayList();
list.add("사과");
list.add(123);  // 문자열 + 숫자 섞임 ❌

// 꺼낼 때 직접 형변환해야 함
String fruit = (String) list.get(0);

```

---

### 제네릭 적용 버전 (현재 방식)

```java
List<String> list = new ArrayList<>(); // String만 허용
list.add("사과");
list.add("샤인머스켓");
// list.add(123); // ❌ 컴파일 에러

String fruit = list.get(0); // 형변환 없이 안전하게 사용 가능

```

---

### 제네릭의 형태

| 형태 | 의미 | 예시 |
| --- | --- | --- |
| `<E>` | Element (요소 타입) | `List<String>` |
| `<K, V>` | Key, Value 타입 | `Map<String, Integer>` |

---

### 제네릭 적용 예시로 다시 보기

```java
// List<String> : 문자열만 담는 리스트
List<String> cart = new ArrayList<>();
cart.add("사과");
cart.add("샤인머스켓");

// Set<String> : 문자열만 담는 집합
Set<String> limited = new HashSet<>();
limited.add("이벤트 상품");

// Map<String, String> : 문자열 Key, 문자열 Value
Map<String, String> store = new HashMap<>();
store.put("과일코너", "사과");
store.put("냉동코너", "하겐다즈");

```

---

## ArrayList vs LinkedList

| 비교 항목 | ArrayList | LinkedList |
| --- | --- | --- |
| 내부 구조 | 배열 기반 | 노드 기반 (연결 리스트) |
| 접근 속도 | 빠름 (인덱스로 접근) | 느림 (순차 접근) |
| 삽입/삭제 | 느림 (요소 이동 발생) | 빠름 (링크 연결만 변경) |
| 메모리 구조 | 연속된 공간 | 분산된 공간 |
| 사용 예시 | 조회가 많을 때 | 삽입/삭제가 많을 때 |

---

### 예시 코드

```java
List<String> arrayList = new ArrayList<>();
arrayList.add("A");
arrayList.add("B");

List<String> linkedList = new LinkedList<>();
linkedList.add("A");
linkedList.add("B");

```

💡 **Tip:**

조회 위주 → `ArrayList`

삽입/삭제 많음 → `LinkedList`

---

## HashMap 내부 구조

데이터를 Key–Value로 저장하고,

Key를 빠르게 찾기 위해 **해시(Hash)** 알고리즘을 사용

---

### HashMap 동작 원리

1️⃣ `put("apple", 10)` 호출

2️⃣ `"apple"`의 해시코드(hashCode) 계산

3️⃣ 해당 해시코드로 버킷(bucket) 위치 결정

4️⃣ 값(10)을 해당 위치에 저장

```
hash("apple") → index 2
[0] null
[1] null
[2] ("apple", 10)
[3] null

```

---

### Hash 충돌 발생 시

- 다른 Key가 같은 인덱스로 해시됨 → **LinkedList로 연결**
- Java 8부터 충돌이 많아지면 **Tree 구조**로 전환 → 속도 개선

---

### 요약 정리

| 항목 | 설명 |
| --- | --- |
| 검색 속도 | 매우 빠름 (평균 O(1)) |
| Key 중복 | 불가능 (덮어쓰기) |
| Value 중복 | 가능 |
| 내부 구조 | 해시테이블 (배열 + 연결리스트) |
| 사용 예 | ID → 정보, 이름 → 점수 등 |

---

### 예시 코드

```java
Map<String, Integer> scores = new HashMap<>();
scores.put("태훈", 95);
scores.put("민수", 88);
scores.put("태훈", 100); // Key 중복 → 덮어쓰기

System.out.println(scores.get("태훈")); // 100

```

---

## 컬렉션과 JDBC의 관계

> JDBC로 DB 데이터를 가져오면,
> 
> 
> 자바에서는 **컬렉션(List/Map)** 으로 담아서 관리한다.
> 

| 상황 | 사용하는 컬렉션 | 이유 |
| --- | --- | --- |
| 여러 회원 조회 | `List<User>` | 여러 행(row) 저장 |
| 컬럼명–값 매핑 | `Map<String, Object>` | Key=컬럼명, Value=값 |
| 전체 결과 | `List<Map<String, Object>>` | 다차원 구조 저장 |

---

### JDBC 예시 코드

```java
List<User> userList = new ArrayList<>();

while (rs.next()) {
    User user = new User(
        rs.getLong("id"),
        rs.getString("name"),
        rs.getString("email")
    );
    userList.add(user);
}

```

---

## 함께 알아두면 좋은 개념

| 주제 | 설명 |
| --- | --- |
| **Iterator** | 컬렉션 요소를 순서대로 탐색 |
| **Collections 클래스** | 정렬, 섞기, 반전 등 유틸 기능 |
| **Stream API** | for문 없이 필터링·정렬·집계 수행 |
| **Concurrent Collections** | 멀티스레드 환경에서 안전한 컬렉션 (`ConcurrentHashMap` 등) |

---

## 전체 요약

Java 컬렉션(Collection Framework) 은

데이터를 담고, 정렬하고, 탐색하고, 관리하는 **자바의 핵심 도구 세트**

**Generic** 으로 타입 안정성을 확보하고,

**List / Set / Map** 으로 데이터 성격에 맞게 관리한다.

실무에서는 JDBC, Stream, JSON, API 응답 구조 등

거의 모든 데이터 흐름의 중심에 **컬렉션**이 있다.